# Build artifacts
_build/
erl_crash.dump
*.beam
*.ez

# Dependencies
deps/

# Documentation
/doc/

# Dialyzer PLTs
priv/plts/

# Environment
.env
.env.local

# Local toolchain pin (asdf). CI pins via setup-beam (elixir 1.18 / otp 27);
# a committed pin would diverge, so this stays developer-local.
.tool-versions

# OS
.DS_Store
*.DS_Store

# AI Tools
/.serena/
/.expert/
/.gemini/
/.cursor/
/.opencode/
/.antigravity/
/.codex/
/.claude/
/.agent/
/.aider/

# Superpowers lifecycle artifacts (specs, plans, handoffs, reviews) — local only, never tracked
/docs/superpowers/

